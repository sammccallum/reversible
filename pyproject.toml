[project]
name = "reversible"
authors = [
    {name = "Sam McCallum", email = "sm2942@bath.ac.uk"}
]
version = "0.0.1"
dependencies = [
    "jax>=0.4.28",
 "jaxtyping>=0.2.24",
 "equinox>=0.11.2",
 "matplotlib>=3.10.0",
]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
